rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // 🔓 開發用規則：允許任何使用者讀寫 Storage
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
