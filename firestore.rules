rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 本機 Emulator 測試期間：開放讀寫（請勿用於正式環境）
    match /{document=**} {
      allow read, write: if true;
    }
  }
}